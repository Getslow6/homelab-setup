# Homelab setup
Setup the debian container by running the following script in the Proxmox shell:
```
bash -c "$(wget -qO- https://raw.githubusercontent.com/Getslow6/homelab-setup/main/debian_setup.sh)"
```
